import React from 'react';
import ReactDOM from 'react-dom/client';
import { UserDashboardDemo } from './pages/UserDashboardDemo';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UserDashboardDemo />
  </React.StrictMode>,
);
